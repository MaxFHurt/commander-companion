# Commander Companion V0.5.2 — Visual Master Lock

The file `VISUAL-MASTER-V0.5.2.png` is the visual authority for the in-game screen. Layout geometry is frozen unless the user explicitly unlocks it in conversation.

Locked details include active-player proportions, commander card placement/aspect ratio, commander-tax badge, public tax summary, status position, two mana rows with six visible mana types, battlefield vertical allocation, 5:7 battlefield cards with horizontal-only overflow, icon-only Tap/Untap All control in the battlefield top-right, compact zone strip, action-tile sizing/state treatment, bottom utility dock, and opponent summary/battlefield orientation.

After any future UI implementation, compare the rendered game screen against this master before calling the build complete.
